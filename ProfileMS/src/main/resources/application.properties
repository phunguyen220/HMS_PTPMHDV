spring.application.name=ProfileMS

server.port=9100
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/profiledb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:TaM123456789@}

eureka.client.register-with-eureka=true
eureka.client.service-url.defaultZone=${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#eureka.client.serviceUrl.defaultZone=http://172?.31.1.105:8761/eureka/

eureka.instance.prefer-ip-address=true

spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.cache.redis.time-to-live=3600000
